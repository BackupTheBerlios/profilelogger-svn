from PatternLegendItem import *

class ColorLegendItem(PatternLegendItem):
    def __init__(self, parent, scene, rect, pos, font, color):
        PatternLegendItem.__init__(self, parent, scene, rect, pos, font, color)
